# 规格评审：多表联合查询

## Status

ready

## 结论

规格完整、决策明确、无阻塞项。最大风险是 LLM JOIN 条件的语义准确性（已记录为 P1）。

## 阻塞项

无

## 风险清单

| 等级 | 风险 | 触发条件 | 影响 | 证据/缺口 | 建议动作 | Owner | 截止点 |
|------|------|---------|------|----------|---------|-------|--------|
| P1 | LLM 生成的 ON 条件语义错误 | JOIN 路径存在但 LLM 理解偏差 | 查询返回错误结果，用户未察觉 | 无法自动检测语义正确性 | 实现后手工测试 5-10 条 JOIN 查询，验证 ON 条件；prompt 中强调"必须使用提供的 JOIN 路径" | dev | 开发完成后 |
| P2 | Neo4j 不可用时无 JOIN 路径 | Neo4j 未启动或图谱未构建 | LLM 凭表结构自己猜 JOIN 条件 | 图谱刷新可能失败（已知 embedding 服务不稳定） | 降级方案：无路径时 prompt 不注入，LLM fallback | dev | — |
| P2 | "最多 3 张表"表述模糊 | — | 可能理解为 3 个 JOIN（4 表）或 3 张表（2 JOIN） | refine.md 原文 | **建议改写**：统一为"最多涉及 3 张表（即最多 2 个 JOIN 关键字）" | — | 实现前 |

## 需要补充的问题

无——所有决策点已在 refine 阶段确认。

## 建议改写

refine.md 第 13 行 "最多 JOIN 3 张表" → "最多涉及 3 张表（即 SQL 中最多出现 2 个 JOIN 关键字）"，避免实现时歧义。

## 验证要点

- [ ] `SELECT ... FROM a JOIN b ON ... LIMIT 100` 通过安全校验
- [ ] `SELECT ... FROM a JOIN b JOIN c JOIN d ... LIMIT 100` (4 表) 被拒绝
- [ ] 含子查询 `SELECT ... FROM (SELECT ...)` 被拒绝
- [ ] 图谱路径注入后 LLM 生成的 ON 条件正确
- [ ] Neo4j 不可用时降级到纯 LLM 推断
