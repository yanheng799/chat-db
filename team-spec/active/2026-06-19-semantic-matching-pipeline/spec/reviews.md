# 规格评审 — 语义匹配 + 单步查询核心链路（Phase 5）

- **slug**：`2026-06-19-semantic-matching-pipeline`
- **评审日期**：2026-06-19
- **评审对象**：`spec/refine.md`（5 轮细化完成版）
- **评审依据**：设计文档 §八 语义匹配 + §十 安全与审核、开发计划 Phase 5、Phase 2 LLM 治理先例（不下发原始行）、Phase 3/4 已实现（PR #22/#29）
- **Status**：`ready`（无 P0、P1 为 LLM 调用次数管控——可在 PRD/issue 阶段跟踪）

## 结论

规格可进入 PRD 固化：5 轮细化覆盖范围收编（3.4 热词 + 5.1–5.5 全含）、管道顺序、审核阻断、热词规模、安全/SQL 故障策略，验收口径可观察且瞄准「首次可演示里程碑」。无 P0、无阻塞 PRD 固化的 P1。最大风险来自**「性能与成本」**维度——Phase 5 单次查询可能调用 LLM 3–4 次（语义兜底+SQL 生成+安全重生成+结果总结），需在实现期设调用次数上限与超时管控。

## 阻塞项

无（不阻塞 PRD 固化）。

## 风险清单

| 等级 | 风险 | 触发条件 | 影响 | 证据/缺口 | 建议动作 | Owner | 截止点 |
|---|---|---|---|---|---|---|---|
| P1 | LLM 调用次数过多（3–4 次/查询） | 每次用户查询 | 延迟高（3-4×LLM 延迟≈10-15s）、成本大（token 消耗） | refine 标注了 3 个 LLM 调用点（语义兜底+SQL 生成+结果总结）+ 安全失败重生成可能加一次；无次数上限 | PRD 设单次查询 LLM 调用上限（如 5 次），超限降级（跳结果总结、直接返回表格）；LLM 调用并行化（语义兜底与结果总结可并行吗？否——依赖关系是串行的） | 实现者 | 开发时 |
| P2 | 结果总结 LLM 外发查询结果与 Phase 2 治理冲突 | 查询返回行级数据 + 用户触发结果总结 | Phase 2 L2 治理——不下发原始业务数据行；Phase 5 结果总结需要把查询结果发给 LLM 才能包装成自然语言 | refine 标注 P3：V1 结果总结只发聚合值；行级结果不总结、直接返回表格；Phase 2 先例可沿用 | PRD 显式写：结果总结 LLM 仅对聚合查询（COUNT/SUM/AVG）的结果做包装；行级查询返回表格、不经 LLM | 实现者 | 开发时 |
| P2 | 「单步查询」约束仅靠 prompt 不可靠 | LLM 生成了含 JOIN 的多表 SQL | 安全校验未必能区分「必要 JOIN」和「违规多步」——精确拦截困难 | 开发计划没描述「单步」的强制机制，只靠 prompt 约束 | PRD 明确：V1 安全校验拦截多表查询作为「过度复杂」——若语义匹配识别到多个表且需 JOIN，阻断并告知用户「V1 仅支持单表查询，多表 JOIN 请等待 Phase 6」 | @yanheng | 进 PRD 前 |
| P2 | 向量检索延迟影响查询体验 | 每次查询调用 Phase 3 向量检索 | `consistency_level="Strong"` + 网络 → 可能增加 200-500ms | Phase 3 向量检索用了 Strong consistency | 保持 Strong；性能优化延 Phase 11 | 实现者 | Phase 11 |
| P2 | 热词词典 V1 命中率低导致频繁走 LLM 兜底 | 用户用语超出现有 10-15 条热词 | LLM 调用次数增加，延迟与成本上升 | 热词 V1 是小规模精选，大部分查询会走第 3/4 层 | V1 接受较高 LLM 调用频率；Phase 5/6 联调时收集语义匹配命中率，迭代补充热词 | @yanheng | Phase 5 联调后 |
| P3 | 管道串联的异常传播（任一步失败是否终止？） | 向量检索超时 / 标准化器抛错 | 管道不确定：是否终止并报错？还是跳过失败步骤继续？ | refine 定义了个别故障策略（安全失败重生成、SQL 失败只报），但未定义全局管道异常传播规则 | PRD 补：任一不可恢复步骤失败 → 终止并返回友好错误；可恢复步骤（如向量检索超时→回退到 LLM 兜底）→ 降级继续 | @yanheng | PRD 时 |

## 需要补充的问题

1. 结果总结 LLM 外发治理：V1 是否确认「只发聚合值、不发原始行」？（P2，不阻塞 PRD）
2. 「单步」约束的强制机制：靠 prompt + 安全校验拦截多表查询是否足够？（P2，PRD 时明确）

## 建议改写（供 PRD 固化时补入，不改 refine.md）

- **结果总结治理**：补「V1 仅对聚合查询结果做自然语言总结；行级查询直接返回表格、不经过 LLM」。
- **单步约束**：补「安全校验拦截含 >1 个 table 的 FROM 子句的 SQL（若语义匹配识别到多表且需 JOIN），阻断并告知用户。
- **管道异常传播**：补全局规则（不可恢复→终止+报错；可恢复→降级继续）。
- **LLM 调用上限**：补 `pipeline_llm_max_calls`（默认 5），超限降级。

## Change Log

- 2026-06-19：首次评审。对照设计文档 §八/§十、Phase 2–4 现实（PR #17/#22/#29 均已 merge/pending）。5 轮细化决议完整、瞄准首次可演示里程碑。无 P0、P1 为 LLM 调用次数；4 项 P2（结果总结治理、单步约束、向量延迟、热词命中率）+ 1 项 P3（管道异常传播）。结论 `ready`。
