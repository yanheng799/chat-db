# 规格评审 — 错误自愈系统（Phase 7）

- **slug**：`2026-06-19-error-self-healing`
- **评审日期**：2026-06-19
- **评审对象**：`spec/refine.md`（4 轮细化完成版）
- **评审依据**：设计文档 §五、dev plan Phase 7、Phase 1 同步引擎已实现、Phase 3/5/6 已实现（PR #22/#36/#40）
- **Status**：`ready`（无 P0、P1 为自愈超时——可在 PRD 阶段跟踪）

## 结论

4 轮细化覆盖全 5 子领域，验收口径可观察。无 P0、无阻塞 PRD 固化的 P1。最大风险来自「性能与稳定性」——独立自愈 Agent 的 60s 超时 + 跨表自救对 Phase 3 的强依赖 + 分类器正则覆盖不全。

## 阻塞项

无。

## 风险清单

| 等级 | 风险 | 触发条件 | 影响 | 证据/缺口 | 建议动作 | Owner | 截止点 |
|---|---|---|---|---|---|---|---|
| P1 | 自愈 Agent 60s 硬超时 | 元数据同步耗时 >60s | 用户被挂起等待、最终超时返回失败 | refine 设了 60s 硬超时但未定义「超过后怎么告知用户」 | PRD 补：超时后返回「自动修复进行中，请稍后重试」+ 后台继续自愈 | 实现者 | 开发时 |
| P2 | 错误分类器依赖正则解析 | 不同 PG/MySQL 版本的错误消息格式不一致 | 分类错误→错误的自愈策略→浪费修复次数 | 设计文档只列了枚举类型，没给正则模式 | PRD 补：按 `pgcode`(PG) / `errno`(MySQL) 字段做精确分类，正则仅作兜底 | 实现者 | 开发时 |
| P2 | 跨表自愈对 Phase 3 的强依赖 | 向量检索超时或图谱不可用 | 自愈链中一步失败→额外失败→浪费一次重试 | Phase 3 已是 PR #22，但向量检索有超时降级（Phase 5 接受了） | 自愈 Agent 对 Phase 3 调用也做降级：超时→跳过跨表策略→进入下一策略 | 实现者 | 开发时 |
| P2 | `healing_records` 只追加不修剪 | 长期运行 | 表膨胀 | refine 已注明「Phase 11 加清理」，但 V1 无上限 | PRD 加：V1 仅追加，后续迭代加保留策略（如最近 N 天） | @yanheng | Phase 11 |
| P3 | 独立 Agent 异步接管增加延迟 | 每次 SQL 失败 | 相比 Phase 5 管道内置重试多一层异步开销 | Phase 5 原本同步报错→立即返回；独立 Agent 需事件传递 | V1 接受额外延迟；未来可对比内置 vs 独立的性能数据后调整 | — | Phase 7 联调后 |

## 建议改写（供 PRD 时补入，不改 refine.md）

- **超时告知**：补「60s 后返回用户可理解的进度信息而非静默错误」。
- **分类器策略**：补「优先用数据库错误码分类（pgcode / errno），正则仅作兜底」。
- **跨表降级**：补「Phase 3 调用超时→跳过跨表策略→进入下一种修复策略」。

## Change Log

- 2026-06-19：首次评审。4 轮细化决议完整、复用 Phase 1/3/6 已实现能力。无 P0、P1 仅 60s 超时；3 项 P2 + 1 项 P3。结论 `ready`。
